import React from "react";

export default class Listcount extends React.Component{

    render(){
        return<Text style={{position:"absolute",marginTop:height-height/16,marginLeft:width/2.9,borderRadius:10,backgroundColor:"#ff6363",color:"white",width:width/25,height:width/25,fontSize:11  ,textAlignVertical:"top",textAlign:"center"}} onPress={()=>{AsyncStorage.getItem('key-1').then((value) => erf =value);console.log(erf)}}>{erf.length}</Text>
    }

}